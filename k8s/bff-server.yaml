apiVersion: v1
kind: Service
metadata:
  name: bff-server
  namespace: abyul
spec:
  selector:
    app: bff-server
  ports:
    - name: http
      port: 4000
      targetPort: 4000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bff-server
  namespace: abyul
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bff-server
  template:
    metadata:
      labels:
        app: bff-server
    spec:
      containers:
        - name: bff-server
          image: abyul/bff-server:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4000
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: abyul-config
                  key: NODE_ENV
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: abyul-config
                  key: BFF_PORT
            - name: API_GATEWAY_GRPC_URL
              valueFrom:
                configMapKeyRef:
                  name: abyul-config
                  key: API_GATEWAY_GRPC_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 30
            periodSeconds: 20
