apiVersion: v1
kind: Service
metadata:
  name: bff-server
  namespace: vox2vocal
spec:
  selector:
    app: bff-server
  ports:
    - name: http
      port: 4000
      targetPort: 4000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bff-server
  namespace: vox2vocal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bff-server
  template:
    metadata:
      labels:
        app: bff-server
    spec:
      containers:
        - name: bff-server
          image: vox2vocal/bff-server:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4000
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: vox2vocal-config
                  key: NODE_ENV
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: vox2vocal-config
                  key: BFF_PORT
            - name: API_GATEWAY_GRPC_URL
              valueFrom:
                configMapKeyRef:
                  name: vox2vocal-config
                  key: API_GATEWAY_GRPC_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 30
            periodSeconds: 20
